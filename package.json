{
  "name": "xposure-online-root",
  "version": "1.0.0",
  "description": "Prototype for Xposure Inc.",
  "main": "index.js",
  "scripts": {
    "build": "npm install --force && npm install --force --prefix frontend && npm install --force --prefix backend && npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "copyfiles": "^2.4.1"
  }
}
